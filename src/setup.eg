
require:
   chalk


async quaint-setup(config, instructions) =
   url = "https://github.com/breuleux/quaint-autoprefixer"
   print 'Plugin documentation: {chalk.bold.cyan(url)}'
   data = config.plugins["autoprefixer"] or= {=}


provide = quaint-setup
